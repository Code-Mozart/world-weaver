generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Rectangle {
  uuid   String @id @default(uuid())
  x      Float
  y      Float
  width  Float
  height Float
}
